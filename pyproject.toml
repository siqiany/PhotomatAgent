[project]
name = "photomatagent"
version = "0.5.0"
description = "A minimal, extensible Scientific Agent Runtime for materials science research."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.7",
    "typer>=0.12",
    "rich>=13.7",
    "prompt-toolkit>=3.0",
    "python-dotenv>=1.0",
    "openai>=1.100",
    "anthropic>=0.60",
]

[dependency-groups]
dev = [
    "mypy>=1.11",
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
]

[project.scripts]
photomatagent = "photomatagent.cli.app:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/photomatagent"]

[tool.pytest.ini_options]
addopts = "--capture=no"
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.hatch.build.targets.sdist]
include = ["skills/"]
