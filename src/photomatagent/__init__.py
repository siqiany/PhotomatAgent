"""MatAgent: a minimal Scientific Agent Runtime.

The runtime is a self-contained agent loop that emits typed events.
The CLI, tests, or any future consumer subscribe to that event stream.
"""

__version__ = "0.2.0"
